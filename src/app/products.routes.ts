import { Routes } from '@angular/router';
import { ProductsComponent } from './products.component'; // Exemplo de um componente

export const productsRoutes: Routes = [
  { path: '', component: ProductsComponent }, // Exemplo de rota
  // Adicione outras rotas conforme necessário
];
